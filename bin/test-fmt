#!/usr/bin/env bash
# Test nufmt formatting on a string
# Usage: test-fmt 'ls | sort-by name'

set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "Usage: test-fmt '<nushell code>'" >&2
    exit 1
fi

echo "$1" | cargo run -q -p nufmt-cli -- --stdin
